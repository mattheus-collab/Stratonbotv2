// Arquivo de rotas principais
// Este arquivo será usado para organizar as rotas da aplicação

const express = require('express');
const router = express.Router();

// Exemplo de rota futura
// router.get('/exemplo', (req, res) => {
//   res.json({ mensagem: 'Rota de exemplo' });
// });

module.exports = router;
